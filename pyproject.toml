[project]
name = "omscs-prep"
version = "0.1.0"
description = "Prep work for Georgia Tech's OMSCS (Machine Learning specialization) — math, algorithms, and ML fundamentals ahead of Spring 2027."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
